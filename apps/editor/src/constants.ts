/** Attribute set on <html> once the shell has rendered and is interactive. */
export const READY_ATTRIBUTE = 'data-app-ready';

/** Performance mark emitted at the same moment {@link READY_ATTRIBUTE} is set. */
export const READY_MARK = 'imagi3:ready';

/** Mount point declared in index.html. */
export const APP_ROOT_ID = 'app';

/** Query parameter used only in development builds to inject audit faults. */
export const FAULT_PARAM = 'plant';
